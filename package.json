{
  "name": "toodles",
  "license": "GPL-3.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs": "cd packages/docs  && yarn run dev",
    "app": "cd packages/app && yarn run dev",
    "app:electron": "cd packages/app && yarn run electron:dev"
  },
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
